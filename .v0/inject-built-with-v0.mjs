#!/usr/bin/env node
// This script is run during the Vercel build process
// It's a no-op placeholder that allows the build to proceed
console.log("[v0] Build injection script executed");
process.exit(0);
